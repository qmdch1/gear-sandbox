# gear-sandbox 로드맵

## 비전

처음엔 "기어가 어떻게 맞물려 돌아가는지 보여주는 교육용 샌드박스"로 시작했지만,
최종 목표는 더 큽니다: **여러 부품을 자유롭게 연결해서 자동차 뼈대나 다양한 기계를
직접 조립해볼 수 있는 범용 기계 조립 샌드박스**. 기어는 그중 하나의 부품 카테고리일
뿐이고, 앞으로는 순수 구조용 부품(뼈대, 축, 커플링)과 동력 전달 부품(체인/벨트)까지
아우릅니다.

이 문서는 사용자가 매번 개별 요청을 하지 않아도 이어서 작업할 수 있도록, 지금까지
정한 방향과 다음에 할 일들을 정리해둔 것입니다. 큰 방향이 바뀌지 않는 한, 아래
순서대로 판단해서 진행합니다.

## 지금까지 확정된 설계 원칙

- **"실제로 안 되면 시뮬레이션에서도 안 돼야 한다"** — 평기어-헬리컬, 베벨-헬리컬처럼
  실제 기계에서 안 맞물리는 조합은 시뮬레이션에서도 연결을 막는다. 반대로 실제로
  가능한데 막혀있던 것(웜 동력 입력, 헬리컬 동력 입력)은 찾아서 뚫는다.
- **빠르게보다 제대로** — 한 번에 여러 부품을 대충 만들기보다, 하나씩 수학적으로
  검증하고 실제 화면으로 확인하고 나서 다음으로 넘어간다.
- **파라메트릭 유지** — 이빨 수·모듈을 사용자가 바꿔도 정확히 맞물려야 하므로, 고정된
  3D 에셋(GLTF 등)을 가져다 쓰는 대신 절차적 지오메트리를 계속 다듬는다.
- **모든 변경은 수학적 검증 + (가능하면) 실제 화면 확인 후 커밋+푸시.** 브라우저 패널이
  화면에 안 떠 있으면 실시간 렌더링 확인이 안 되는 환경 제약이 있어서, 그럴 땐 정직하게
  "이번엔 화면으로 확인 못 했다"고 밝힌다.

## 지금까지 만든 것 (완료)

- 인볼류트 이(spur/helical/crank), 원뿔 경사면에 이가 파고든 베벨, 나선 나사산이
  있는 웜 — 전부 이빨 물림 위치·회전 방향까지 수학적으로 검증.
- 축 방향 토글(V 키) — 베벨/웜을 가로↔세로로 세울 수 있음.
- 높이 조절(Shift+드래그).
- 바퀴(허브+스포크+타이어, 부위별 다른 색 렌더링 구조 포함).
- 동력원 통합(손잡이/배터리/콘센트 → 크랭크 하나), 헬리컬·웜 동력 입력 버그 수정.
- 실제 재질에 가까운 색상(참고 사진 기반), 환경맵 반사, 게이지 계기판.
- 서버 저장/불러오기 단순화(저장 하나, 불러오기 하나).

## 다음 단계 (우선순위 순)

### 1. 동력전달축 / 커플링 로드 (다음 작업)
두 부품을 떨어진 채로 연결하는 첫 번째 "2점 연결" 부품. 지금 구조는 부품 하나당
위치(position)가 하나뿐이라, 이걸 제대로 하려면:
- `GearInstance`에 두 번째 연결점(`position2`류 필드) 추가.
- 양쪽 끝이 각각 다른 기어의 축에 결합(coupling, ratio=1)되도록 `evaluatePair` 확장.
- 렌더링: 두 점을 잇는 로드 형태(길이/방향이 매번 다름 — 기존의 "축 하나로 정해지는
  고정 형태" 관례에서 벗어나는 첫 사례).
- 드래그 UX: 끝점 하나씩 따로 옮길 수 있어야 실용적 — 어느 끝을 잡았는지 판별하는
  로직 필요.

### 2. 구조용 프레임 부품 (자동차 뼈대를 만들려면 필요)
기어와 달리 "돈다"는 개념이 없는, 순수 강성 연결용 부품들:
- **빔/샤시 바(直선 프레임 부재)** — 두 점을 잇는다는 점에서 커플링 로드와 렌더링
  구조를 공유할 수 있음.
- **브라켓/조인트(고정 이음쇠)** — 여러 부품을 한 점에 강체로 고정.
- **차축(axle)** — 바퀴 두 개를 양 끝에 달고 회전하는 긴 축. 바퀴(완료)와 결합축
  개념(이번에 고친 웜 동력 입력과 유사)을 재사용 가능.

### 3. 체인/벨트 구동
두 스프로킷/풀리를 떨어진 채로 동력 전달(자전거 체인 방식). 커플링 로드보다 범위가
더 큽니다 — 매 프레임 두 점 사이를 잇는 유연한(휘어지는) 형태를 다시 계산해야 하는데,
지금까지 부품들은 전부 "한 번 만들면 안 바뀌는 고정 형태"라서, 여기서 처음으로
"위치가 바뀔 때마다 형태 자체가 바뀌는" 렌더링이 필요합니다. 셋 중 가장 나중에.

### 4. 성능
부품 수가 늘어나면 챙길 것:
- 같은 타입+이빨수+모듈 조합의 지오메트리는 캐싱해서 재사용(지금은 기어마다 매번
  새로 만듦).
- `mergeGeometries`가 매번 배열을 순회하며 새 Float32Array를 만드는데, 부품 수가
  많아지면 이 부분도 재검토.
- 드로우콜이 늘어나면 인스턴싱(InstancedMesh) 고려 — 단, 부품마다 색/텍스처가 달라질
  수 있어서(내구도 색상 등) 그룹핑 전략이 필요.

## 참고

- 이 문서는 살아있는 문서입니다 — 방향이 바뀌거나 우선순위가 바뀌면 이 문서도 같이
  고칩니다.
- 커밋 메시지에 항상 "왜"를 남기는 지금 방식(실제로 검증한 내용, 남은 한계)을 계속
  유지합니다.
