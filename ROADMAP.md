# gear-sandbox 로드맵

## 비전

처음엔 "기어가 어떻게 맞물려 돌아가는지 보여주는 교육용 샌드박스"로 시작했지만,
최종 목표는 더 큽니다: **여러 부품을 자유롭게 연결해서 자동차 뼈대나 다양한 기계를
직접 조립해볼 수 있는 범용 기계 조립 샌드박스**. 기어는 그중 하나의 부품 카테고리일
뿐이고, 앞으로는 순수 구조용 부품(뼈대, 축, 커플링)과 동력 전달 부품(체인/벨트)까지
아우릅니다.

이 문서는 사용자가 매번 개별 요청을 하지 않아도 이어서 작업할 수 있도록, 지금까지
정한 방향과 다음에 할 일들을 정리해둔 것입니다. 큰 방향이 바뀌지 않는 한, 아래
순서대로 판단해서 진행합니다.

## 지금까지 확정된 설계 원칙

- **"실제로 안 되면 시뮬레이션에서도 안 돼야 한다"** — 평기어-헬리컬, 베벨-헬리컬처럼
  실제 기계에서 안 맞물리는 조합은 시뮬레이션에서도 연결을 막는다. 반대로 실제로
  가능한데 막혀있던 것(웜 동력 입력, 헬리컬 동력 입력)은 찾아서 뚫는다.
- **빠르게보다 제대로** — 한 번에 여러 부품을 대충 만들기보다, 하나씩 수학적으로
  검증하고 실제 화면으로 확인하고 나서 다음으로 넘어간다.
- **파라메트릭 유지** — 이빨 수·모듈을 사용자가 바꿔도 정확히 맞물려야 하므로, 고정된
  3D 에셋(GLTF 등)을 가져다 쓰는 대신 절차적 지오메트리를 계속 다듬는다.
- **모든 변경은 수학적 검증 + (가능하면) 실제 화면 확인 후 커밋+푸시.** 브라우저 패널이
  화면에 안 떠 있으면 실시간 렌더링 확인이 안 되는 환경 제약이 있어서, 그럴 땐 정직하게
  "이번엔 화면으로 확인 못 했다"고 밝힌다.

## 지금까지 만든 것 (완료)

- 인볼류트 이(spur/helical/crank), 원뿔 경사면에 이가 파고든 베벨, 나선 나사산이
  있는 웜 — 전부 이빨 물림 위치·회전 방향까지 수학적으로 검증.
- 축 방향 토글(V 키) — 베벨/웜을 가로↔세로로 세울 수 있음.
- 높이 조절(Shift+드래그).
- 바퀴(허브+스포크+타이어, 부위별 다른 색 렌더링 구조 포함).
- 동력원 통합(손잡이/배터리/콘센트 → 크랭크 하나), 헬리컬·웜 동력 입력 버그 수정.
- 실제 재질에 가까운 색상(참고 사진 기반), 환경맵 반사, 게이지 계기판.
- 서버 저장/불러오기 단순화(저장 하나, 불러오기 하나).
- **동력전달축(커플링 로드)** — 첫 "2점 연결" 부품. `position2` 필드, 양 끝 각각
  다른 기어에 축 결합(`evaluatePair`/`idealConnectionDistance` 확장), 길이가
  매번 달라지는 렌더링(`scale.z`로 늘이기), 평소엔 통째로 드래그·Ctrl+드래그로
  반대쪽 끝만 따로 이동.
- 베벨/웜 축 방향 토글(V 키) — 베벨-베벨 쌍처럼 이전엔 만들 방법 자체가 없던 조합도
  이제 가능. 이후 **모든 부품**으로 일반화(`toggledAxis`가 타입 구분 없이 동작).
- **구조용 빔** — 회전을 전혀 전달하지 않는 순수 강체 연결 부품. 동력전달축과 렌더링
  구조(2점, `scale.z` 늘이기)는 공유하지만 `MeshEdge.kind: "structural"`이라는 새
  종류로 구분되어 `propagateRotation`에서 아예 무시됨(그래야 회전이 새지 않음).
  동력전달축과 달리 빔끼리는 끝을 맞대어 계속 이어붙일 수 있어(체인처럼) 사각형
  뼈대·트러스 같은 골격을 조립할 수 있음. 팔레트에 "구조" 카테고리 신설(동력전달과
  구분 — 빔은 드라이브트레인 부품이 아니므로).
- **벨트/체인 구동** — 떨어진 두 기어(풀리·스프로킷)를 같은 방향으로 함께 돌리는 부품.
  동력전달축과 데이터 모양(2점, `position2`)은 같지만, 결합의 성격이 다름: 호스트
  기어의 잇수 없이 이빨수만 있으면 되고(축 방향 정렬 요구 없음), 속도 비율이 1:1
  고정이 아니라 두 풀리의 피치 반지름 비율로 정해짐. `evaluatePair`가 새 필드를
  추가하지 않고 기존 "coupling" 종류를 재사용하되, `ratio`에 호스트의 피치 반지름을
  넣어서(방향에 따라 반지름 자체 또는 역수) `propagateRotation`의 기존 ratio/1÷ratio
  왕복 전파 로직이 그대로 각속도↔"벨트 선속도" 단위 변환을 하도록 설계 — 회전 전파
  쪽은 코드 변경이 전혀 필요 없었음.
- **벨트/체인 접선(tangent) 렌더링** — 위 벨트 렌더링을 단순한 중심선 띠 하나에서 실제
  벨트처럼 두 풀리 반지름만큼 벗어난 접선 두 줄로 교체. 점-법선(point-normal) 직선
  형태로 두 원 모두에 접하는 조건(원점1에서 거리=r1, 원점2에서 거리=r2, 같은 쪽)을
  풀면 법선 N = k*u + s*n (k=(r1-r2)/d, s=sqrt(1-k²))이 나오고, 접점은 두 원 모두
  `중심 + 반지름*N`으로 똑같이 구해짐 — 반지름이 다르면 두 줄이 작은 풀리 쪽으로
  모이는(taper) 모양이 되고, 같으면 평행한 두 줄이 됨(실제 스크린샷으로 둘 다 확인).
  단, 이 지오메트리는 두 호스트의 현재 위치·반지름에 따라 매번 달라지므로 (타입,
  이빨수,모듈) 캐시 대상에서 제외하고 연결이 바뀔 때마다 다시 만듦(정점 수가 적어
  비용은 무시할 만함) — `beltEndRadius1`/`beltEndRadius2`라는 순수 렌더링용 필드를
  매 틱마다 `simulation.ts`가 그래프에서 계산해 채워줌(물리 계산과는 독립적).
- **지오메트리 캐싱** — `buildGeometryForType`는 (타입, 이빨수, 모듈)만의 순수 함수라는
  점에 착안해, `geometryCache.ts`가 같은 조합을 요청받으면 새로 만들지 않고 캐싱된
  `BufferGeometry`를 그대로 재사용하도록 함(재질은 내구도 색상이 기어마다 달라야 해서
  캐싱하지 않음, 지오메트리만). 부수 효과로 `mergeGeometries`의 배열 순회 비용도
  기어 개수가 아니라 "실제로 쓰이는 서로 다른 (타입,이빨수,모듈) 조합 수"에만
  비례하게 되어, 로드맵에 있던 "mergeGeometries 재검토" 우려도 사실상 해소됨.
  단, `GearMeshObject.dispose()`가 공유 지오메트리를 실수로 dispose하면 안 되므로
  (다른 기어가 여전히 쓰고 있을 수 있음) 지오메트리는 절대 dispose하지 않고 재질만
  dispose하도록 변경 — 테스트로 확인(dispose 스파이 사용).
- **인스턴싱(InstancedMesh)** — 사용자 요청으로 조건 없이 착수. "belt"만 예외로 두고
  (호스트 위치·반지름에 따라 매번 모양이 바뀌는 접선 지오메트리라 애초에 공유할
  고정된 모양이 없음 — `GearMeshObject`를 그대로 유지) 나머지 모든 타입은 (타입,
  이빨수,모듈)별로 `InstancedGroup` 하나(=드로우콜 하나)를 공유하도록 재설계:
  - `InstancedGroup`(`instancedGroup.ts`) — 용량을 넘으면 배열처럼 2배로 재할당하며
    새 `InstancedMesh`로 교체(씬 부착/해제까지 스스로 처리), 제거는 마지막 인스턴스를
    빈 슬롯으로 옮기는 "swap and pop" 방식(O(1)).
  - `computeInstanceMatrix`(`instanceTransform.ts`) — `GearMeshObject.update()`의
    변환 수식을 그대로 옮긴 게 아니라 독립적으로 다시 구현(이 코드베이스의 기존
    방침대로 — `meshing.ts`의 `idealConnectionDistance`처럼 위험을 나누기 위해).
    두 구현이 실제로 일치하는지는 전용 테스트로 검증.
  - 문제/미리보기 강조 표시는 인스턴스별 `emissive`가 존재하지 않아서(재질이
    공유되므로) 방식이 달라짐: 벨트(개별 메시)는 기존처럼 emissive 덧칠, 나머지
    (인스턴싱된) 부품은 내구도 색상에 강조색을 50% 섞어 넣는 방식으로 대체 — 육안
    경고 신호로는 똑같이 명확하지만, 두 부품군이 시각적으로 살짝 다른 방식으로
    빛난다는 점은 정직하게 남겨둔 트레이드오프.
  - 피킹(레이캐스트로 어느 기어를 클릭했는지 알아내기)이 가장 위험했던 부분 —
    `InstancedMesh` 하나가 여러 기어를 대표하므로 `.name`에 기어 id를 담을 수 없어,
    `SceneSync.resolveHitId(object, instanceId)`가 히트를 실제 기어 id로 되돌려
    풀어주도록 새로 추가하고 `dragControls.ts`가 이걸 통해서만 히트를 해석하게 바꿈.
    실제 브라우저에서 같은 타입/이빨수/모듈의 기어 두 개를 만들어 하나만 드래그해서
    다른 하나는 그대로인지 직접 확인(처음엔 기어의 정중앙 = 축 구멍을 클릭해서
    "히트 없음"으로 착각했던 삽질도 있었음 — 톱니 부분을 클릭하니 정상 동작).
- **사용자 피드백 반영 (UI/연결성/베벨 모양)**:
  - **팔레트 다중 탭** — 카테고리 버튼이 서로를 닫던 아코디언 동작을 없애고, 각자
    독립적으로 토글되도록 변경(여러 카테고리를 동시에 펼쳐둘 수 있음).
  - **부품별 번호 라벨** — `gearLabels.ts`의 `computeGearLabels`가 매 렌더마다
    같은 타입 내 등장 순서로 "평기어 1", "동력원 기어 2" 같은 이름을 매겨서
    `DiagnosticsPanel`이 원시 UUID 대신 이걸 보여주도록 변경(동력원도 당연히
    포함 — 타입 구분 없이 전부 번호가 붙음). 저장 파일 스키마는 그대로 — 매번
    다시 계산하므로 부품이 삭제되면 번호가 밀릴 수 있지만, 아직 삭제 기능이
    없어서 지금은 문제되지 않음.
  - **"미연결"은 문제가 아님** — 방금 놓은 부품이 아직 아무것에도 안 붙어있는
    건 정상 상태이지 잘못된 게 아니라는 피드백을 반영해, `DiagnosticsPanel`을
    "연결 안 된 부품"(중립 스타일)과 "문제 있는 기어"(동력 없음·겹침만, 빨간
    스타일)로 분리. `sceneSync.ts`의 3D 화면 빨간 강조 표시에서도 미연결을
    제외 — 갓 놓인 부품이 빨갛게 물드는 게 더 이상 없음.
  - **베벨 기어 재설계** — 실제 사진(public/parts/bevel.jpg)을 다시 보니 진짜
    베벨 기어는 뾰족한 원뿔이 아니라 두껍고 거의 평평한 "바퀴" 모양에 테두리
    이빨이 달린 형태였음. 기존 `bevelGeometry`가 끝까지 좁아지는 완전한 원뿔
    (`ConeGeometry`)을 만들고 있던 게 근본 원인 — "기어 같지도 않다"는 피드백이
    정확했음. 밑면-윗면 반지름 차이가 두께와 같은(45° 경사, 1:1 베벨 쌍의 표준
    피치 원뿔 각) 짧은 절두원뿔(프러스텀)로 교체, 이빨도 원뿔 전체가 아니라
    이 짧은 두께 전체에 걸쳐 잘라내도록 수정. 실제 화면에서 확인(원뿔 모양 →
    톱니 달린 두꺼운 바퀴 모양으로 확실히 개선).
  - **헬리컬 기어 연결성** — 실제로 동력원(크랭크)에 정확히 겹치게 놓으면 잘
    붙는다는 걸 재확인했지만(회귀 아님), "겹치는 지점"이 호스트 기어의 중심
    한 점뿐이라 눈에 보이는 기어 면적에 비해 맞추기 어려운 목표였음 —
    `dragControls.ts`의 축 결합(coincident coupling) 스냅 허용 오차를 고정
    6유닛에서 호스트의 실제 피치 반지름만큼으로 늘려서, 호스트 기어 원판 위
    어디에 놓아도 스냅되도록 함(`findSnapTarget`/`findShaftEndpointSnap` 둘 다).
    실제 드래그로 재현: 헬리컬을 크랭크 중심에서 7유닛 떨어진 곳에 놓아도
    정확히 겹치는 위치로 스냅되고 "문제 없음"으로 확인됨.
- **"부품 클릭하면 화면(카메라)이 잡히는" 버그 수정** — 실제 원인: 기어 정중앙에는
  축 구멍이 있어서 그 자리를 클릭하면 정밀 레이캐스트가 아무것도 못 맞히고, 그
  클릭이 그대로 OrbitControls로 넘어가 카메라가 움직여버림(부품이 아니라). 사용자
  입장에선 "분명 부품 위를 클릭했는데 화면만 움직인다"로 보임. `pickGearByFootprint`
  (`dragControls.ts`)를 새로 추가: 정밀 레이캐스트가 아무것도 못 맞혔을 때만, 클릭
  지점이 각 부품의 "눈에 보이는 범위"(원형 부품은 중심으로부터의 반지름, 축/빔/벨트
  같은 2점 부품은 중심선까지의 거리) 안에 있는지 확인해서 폴백으로 집어줌 — 실제
  지오메트리를 정밀하게 따라가지 않고 넉넉하게 잡음. 실제 드래그로 재현: 기어 정중앙
  (구멍 자리)을 클릭+드래그해도 이제 기어 자체가 움직임(카메라 아님).
  - 같은 세션에서 "기본 부품도 잘 안나온다"는 피드백도 받았는데, 원인은 코드가
    아니라 개발 서버(Vite HMR)가 여러 번의 편집을 거치며 브라우저 탭에 낡은 모듈
    참조를 남긴 것으로 확인(새 탭으로 열면 12개 부품 전부 정상 표시) — 실제 배포
    환경과는 무관한, 이 세션 특유의 개발 중 현상이었음.
- **확인할 것 목록 클릭 → 깜빡이며 이동** — 부품을 클릭하면 카메라만 그 위치로
  옮기던 것에, 그 부품이 몇 번 반짝였다가 서서히 가라앉는 효과를 추가
  (`sceneSync.ts`의 `flash()`). 사인파 깜빡임(3회) × 전체 페이드아웃 봉투로 계산,
  인스턴싱된 부품은 색상에 섞어 넣고 벨트(개별 메시)는 기존 emissive 방식 그대로
  사용. 실시간 확인은 `performance.now()`를 직접 mock한 단위 테스트로(백그라운드
  탭에서는 `setTimeout` 스로틀링 때문에 브라우저에서 정밀한 타이밍 확인이
  어려웠음 — 100ms 시점 색상 변화 하나는 실제로도 확인됨).
- **부품 담기 탭: 기본값 = 전부 열림 + 마지막 상태 기억** — 카테고리들이 기본으로
  전부 펼쳐진 상태로 시작하고, 사용자가 열고 닫은 걸 `localStorage`에 저장해서
  다음에 열 때 그대로 복원(`persistedOpenState.ts`, `gear-sandbox:palette-open-
  categories` 키). 사이드바에 다른 접고 펼 수 있는 메뉴가 생기면 같은 헬퍼를
  재사용하면 됨(지금은 팔레트 카테고리가 유일하게 그런 구조를 가진 메뉴).
- **크기 조절 게이지바** — 부품을 클릭해서 고르면 사이드바에 슬라이더가 나타나
  `module`을 실시간으로 조절(`sizeControlPanel.ts`). `module`은 이 코드베이스의
  모든 지오메트리 함수가 공통으로 쓰는 순수 크기 배율이라(허브·림·날개·이빨 등
  전부 `module`에 비례) 바퀴든 기어든 같은 슬라이더 하나로 커버됨. 인스턴싱된
  부품은 (타입,이빨수,모듈)로 그룹이 정해지므로, 크기가 바뀌면 옛 그룹에서 빼고
  새 그룹으로 옮기는 로직을 `sceneSync.ts`에 추가(옛 그룹이 비면 통째로 정리) —
  기존엔 "module/teeth는 생성 후 절대 안 바뀐다"는 전제로 그룹을 한 번만
  정했었는데, 이 기능으로 그 전제가 깨져서 새로 대응함. 덧붙여 연결 안 된 벨트가
  계속 미연결 상태로 있으면서 크기만 바뀌는 경우 지오메트리가 갱신 안 되던 구멍도
  같이 메움(`gearMesh.ts`). 실제 화면에서 바퀴·평기어 둘 다 슬라이더로 커졌다
  작아졌다 하는 것 직접 확인(위치는 그대로 유지됨).
- **기본 모듈 0.5로 축소 + 리사이즈 전파** — 새로 놓는 부품의 기본 크기를
  1 → 0.5로 낮춤(`gearFactory.ts`의 `DEFAULT_MODULE`). 그리고 실제 기어들처럼,
  이빨이 서로 맞물리려면(`mesh` 종류 엣지) 반드시 같은 모듈이어야 한다는 원칙에
  따라, 슬라이더로 한 기어를 리사이즈하면 그 기어와 실제로 맞물려 있는 다른
  기어들 전체(체인처럼 연쇄적으로, 축 결합만 된 부하/게이지/바퀴 등은 이빨이
  없어서 제외)에 같은 모듈을 전파하고, 각자 새 모듈에 맞는 올바른 맞물림
  거리로 재배치(`resize.ts`의 `resizeConnectedMeshGroup` — 앵커에서 각 이웃으로
  가는 방향/각도는 유지하고 거리만 새로 맞춤). 순환(cycle)이 있는 맞물림
  그래프(예: 아이들러 기어 세 개가 원형으로 서로 맞물리는 경우)는 한 번의
  BFS로 모든 쌍의 거리를 동시에 만족시킬 수 없는 과잉제약 문제라 완벽하지
  않다는 점은 정직하게 남겨둠(드문 경우라 실제 제약 솔버까지는 만들지 않음).
  이빨 위상(phase) 재정렬은 하지 않음 — 거리는 정확해지지만 톱니-홈 정중앙
  맞춤까지는 다음 다듬기 과제로 남김. 실제 화면에서 맞물린 평기어 두 개로
  확인: 리사이즈한 기어(앵커)는 제자리에 그대로, 맞물린 파트너는 모듈이
  똑같이 바뀌고 정확한 새 거리(20→40)로 이동함.
- **실제로 "자동차 뼈대" 하나 조립해서 저장** — 사용자가 "이 부품들로 뭘 만들 수
  있는지 보여달라, 안 되면 추가해라"고 요청 → 후륜구동 장난감 자동차를 실제로
  만들어서 서버에 저장해둠(14개 부품). 구성: 빔 4개로 40x20 사각 섀시, 각 모서리에
  차축(shaft)+바퀴, 뒤쪽에 크랭크→베벨기어(90도 방향 전환, 실제 자동차의 종감속기어
  원리)→구동축(driveshaft)→뒤 차축. 게이지 하나를 크랭크 축에 얹어서 계기판처럼 회전
  속도 표시. 만들면서 진짜 기능 공백을 하나 발견해서 고쳤음:
  - **샤프트-샤프트 연결(체이닝) 추가** — 지금까지 "빔은 빔끼리 이어붙일 수 있지만
    샤프트는 안 됨(아직)"이었는데, 크랭크에서 멀리 있는 뒤 차축까지 동력을 옮기는
    구동축을 만들려니 정확히 이 기능이 필요했음. 빔의 기존 선례를 그대로 따라
    샤프트끼리도 끝과 끝을 맞대면 연결되도록 `meshing.ts`를 수정(`shaftCouplingEnd`가
    상대방도 샤프트일 때는 그 샤프트 자신의 방향을 기준 축으로 사용).
  - 고치는 과정에서 더 깊은 버그도 하나 발견: `shaftCouplingEnd`가 항상 상대방의
    `position`만 확인하고 `position2`는 한 번도 확인하지 않고 있었음(지금까지는
    상대방이 항상 바퀴/크랭크 같은 "점 하나짜리" 타입이라 문제가 안 됐는데, 샤프트끼리
    연결이 가능해지면서 상대방의 "두 번째 끝"이 닿는 경우를 놓치게 됨) — 상대방의
    양쪽 끝 다 확인하도록 고침. 첫 설계(축을 섀시 모서리에서 살짝 안쪽으로 띄워서
    겹침을 피하려던 시도)에서 이 버그 때문에 섀시와 구동계가 서로 완전히 분리된
    채로 저장됐던 걸 직접 화면에서 "동력 없음: 구조용 빔..." 목록을 보고 발견,
    축을 섀시 모서리와 정확히 겹치도록 재배치해서 해결.
  - 실제 화면에서 최종 확인: "문제 없음"(연결 안 된 것도, 겹침도 없음), 그리고
    각 부품의 실제 회전 속도 값도 직접 읽어서 확인 — 크랭크·베벨·구동축·뒤 차축·
    뒤 바퀴 둘 다 전부 1(같은 속도로 회전), 앞 차축·앞 바퀴는 0(동력 안 감,
    실제 후륜구동차처럼 정상), 빔은 항상 0(구조물이라 회전 없음).

- **좌우 90도 회전(방향키)** — 클릭 후 V 키로는 가로/세로(X/Y) 두 상태만 왔다갔다
  했는데, 차체 너비 축으로 도는 바퀴처럼 Z축까지 필요한 경우가 있어서 방향키(←/→)로
  X→Y→Z를 한 단계씩 도는 `rotatedAxis`를 추가(`gearFactory.ts`). 기존 V 키의
  `toggledAxis`는 그대로 두고 완전히 별개 함수로 유지(이미 자리잡은 동작을 건드리지
  않기 위한 판단). 5개 테스트(정방향/역방향/한 바퀴 원점 복귀/부정확한 축의 최근접
  스냅)로 검증.
- **새 부품 5종 추가** — "쓸만하게 만들어달라"는 요청에 따라, 헬리콥터·탱크 같은
  더 복잡한 조립을 실제로 가능하게 만드는 부품들을 추가:
  - **유니버설 조인트(joint)** — 겉보기엔 동력전달축과 비슷하지만, 축 방향이 서로
    안 맞는(평행하지 않은) 두 샤프트 사이에도 회전을 전달함(`meshing.ts`의
    `jointJoinsAt` — 빔의 "그냥 겹치면 연결" 규칙을 그대로 쓰되, `structural`이
    아니라 회전을 전달하는 `coupling` 엣지를 반환). 실제 자동차 등속 조인트처럼
    서스펜션이 움직여도 동력이 계속 전달되는 구조를 만들 수 있게 됨.
  - **베어링(bearing)** — 부하/게이지/바퀴처럼 결합 전용(이빨 없음) 부착 장치지만
    훨씬 작고 가벼움 — 뼈대 위에서 축을 가볍게 받쳐주는 지점을 표시하는 용도.
  - **스프링(spring)** — 구조용 빔과 똑같이 회전 없이 두 지점을 고정하는
    구조용 로드지만(`STRUCTURAL_ROD_TYPES`로 빔과 함께 일반화), 코일 모양으로
    렌더링되어 "여긴 유연하게 힘을 흡수하는 지점"이라는 의미를 시각적으로 표시 —
    서스펜션 뼈대 조립에 사용.
  - **로터(rotor)** — 팬과 같은 결합 전용 출력 장치지만 날개가 훨씬 길고
    2개뿐 — 헬리콥터 메인/테일 로터 형태.
  - **무한궤도(track)** — 벨트/체인과 물리적으로 완전히 동일한 동력 전달 방식
    (`BELT_LIKE_TYPES`로 일반화해서 `evaluatePair`/`idealConnectionDistance`
    로직을 공유)이지만, 두꺼운 돌기(그루서)가 있는 렌더링으로 탱크 궤도처럼
    보이게 함. 벨트와 마찬가지로 접선(tangent) 지오메트리가 매 프레임 바뀌어서
    인스턴싱 대상에서 제외(`sceneSync.ts`의 `isInstanced`).
  - 5개 타입 모두 `types.ts`/`gearDefs.ts`/`gearFactory.ts`/`gearGeometry.ts`/
    `metalTexture.ts`/`partInfo.ts`/`paletteUI.ts`/`serialize.ts`를 관통해서
    추가하고, 각 계층에 대응하는 테스트를 새로 작성(`gearFactory.test.ts`·
    `meshing.test.ts`·`rotation.test.ts`·`gearGeometry.test.ts`) — 특히 조인트가
    "축이 안 맞는 두 샤프트를 실제로 연결해서 회전을 전달한다"는 핵심 동작은
    `propagateRotation`까지 통째로 돌려서 검증.

## 다음 단계 (우선순위 순)

- 위 5개 신규 부품(특히 조인트·스프링·로터·무한궤도)을 실제로 활용해서 헬리콥터·
  탱크·자동차처럼 서로 다른 원리로 동작하는 여러 조립물을 만들고, 각각 저장해서
  화면으로 직접 검증한다.
- 여러 조립물의 동력 경로(동력원 → 각 출력 부품까지의 총 기어비/실제 회전 속도)를
  한눈에 비교할 수 있는 진단 기능을 추가해서, "이 자동차와 저 자동차 중 어느 쪽이
  더 빠른가/더 힘이 센가" 같은 효율 비교가 가능하게 만든다.

## 참고

- 이 문서는 살아있는 문서입니다 — 방향이 바뀌거나 우선순위가 바뀌면 이 문서도 같이
  고칩니다.
- 커밋 메시지에 항상 "왜"를 남기는 지금 방식(실제로 검증한 내용, 남은 한계)을 계속
  유지합니다.
